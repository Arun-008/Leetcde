class Solution(object):
    def firstPalindrome(self, words):
        """
        :type words: List[str]
        :rtype: str
        """
        for i in range(len(words)):
            rev=str(words[i][::-1])
            if words[i]==rev:
                return words[i]
        return ""
        
