class Solution(object):
    def hasTrailingZeros(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        even = 0
		
        for n in nums:
            if n%2 == 0: 
                even += 1
            if even==2:
                return True
        return False
